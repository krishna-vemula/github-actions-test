name: run shell commands

on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: echo a string
        run: echo "Hello First WorkFlow"
      - name: multiline script
        run: |
          node -v
          npm -v
          java --version